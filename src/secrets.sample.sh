#!/bin/sh

# Sample for credentials.sh
# This can be set up the same as the env_vars.sh file, only with default .gitignore support.
# Place your credentials or secrets here so that they don't get accidentally committed.

SOME_PRIVATE_TOKEN=example_value
